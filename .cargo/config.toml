[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]

[env]
# Prefer vendored OpenSSL to avoid requiring local system install for CI and dev environments
OPENSSL_NO_VENDOR = "0"
